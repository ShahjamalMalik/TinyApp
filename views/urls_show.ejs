<!DOCTYPE html>
<html lang="en">
  <head>
    <title>TinyApp-ShowURL</title>
  </head>
  <body>

    <a href="<%= urls[shortURL] %>"><%= urls[shortURL] %></a>   -   <%= shortURL %>
    </ul>
  </body>
</html>
